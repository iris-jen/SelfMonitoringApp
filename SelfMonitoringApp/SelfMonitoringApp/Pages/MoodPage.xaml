<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:SelfMonitoringApp.Controls"
             mc:Ignorable="d"
             Visual="Material"
             NavigationPage.HasBackButton="true"
             NavigationPage.HasNavigationBar="True"
             Title="Log Mood"
             x:Class="SelfMonitoringApp.Pages.MoodPage">
    <ContentPage.Content>
        <controls:SaveCancelPage CancelCommand="{Binding BackCommand}" SaveCommand="{Binding SaveLogCommand}">
            <Grid Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="{StaticResource RowHeightSlider}"/>
                    <RowDefinition Height="{StaticResource RowHeightSingle}"/>
                    <RowDefinition Height="{StaticResource RowHeightSingle}"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
            
                <!--Rating slider-->
                <controls:HeaderBlock HeaderText="Rate how you feel">
                    <controls:RatingSlider Rating="{Binding OverallMood}" />
                </controls:HeaderBlock>
            
                <!--Emotions entry box-->
                <controls:HeaderBlock Grid.Row="1" HeaderText="Strongest Emotion">
                    <Entry Text="{Binding StrongestEmotion}" BackgroundColor="White" Placeholder="What emotion is most intense?" />
                </controls:HeaderBlock>

                <controls:HeaderBlock Grid.Row="2" HeaderText="Location">
                    <Entry Text="{Binding Location}" BackgroundColor="White" Placeholder="Where were you?" />
                </controls:HeaderBlock>
                
                <!--Descripition Box-->
                <controls:HeaderBlock Grid.Row="3" HeaderText="Description">
                    <Frame Padding="4">
                        <Editor BackgroundColor="White" IsSpellCheckEnabled="True" Text="{Binding Description}" VerticalOptions="CenterAndExpand"
                                Placeholder="How do you feel?  What happened?" AutoSize="TextChanges"/>
                    </Frame>
                </controls:HeaderBlock>

            </Grid>
        </controls:SaveCancelPage>
        </ContentPage.Content>
</ContentPage>