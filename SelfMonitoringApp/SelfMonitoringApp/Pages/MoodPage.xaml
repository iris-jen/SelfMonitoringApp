<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:SelfMonitoringApp.Controls"
             mc:Ignorable="d"
             Visual="Material"
             NavigationPage.HasNavigationBar="False"
             x:Class="SelfMonitoringApp.Pages.MoodPage">
    <ContentPage.Content>
        <controls:PageBackgroundWithHeader HeaderText="Mood Log">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="140"/>
                    <RowDefinition Height="250"/>
                    <RowDefinition Height="200"/>
                     <RowDefinition Height="60"/>
                </Grid.RowDefinitions>
            
                <!--Rating slider-->
                <controls:HeaderBlock HeaderText="Rate how you feel">
                    <Frame Opacity="0.8">
                        <controls:RatingSlider SliderValue="{Binding OveralMood}"/>
                    </Frame>
                </controls:HeaderBlock>
            
                <!--Emotions entry box-->
                <controls:HeaderBlock Grid.Row="1" HeaderText="Emotions">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60"/>
                            <RowDefinition/>
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3.5*"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Entry Text="{Binding NewEmotion}" BackgroundColor="White" Opacity="0.8" Placeholder="Enter an emotion..." />
                            <Button Grid.Column="1" Command="{Binding AddNewEmotion}" Text="+" FontSize="20" Margin="1" Style="{StaticResource ButtonStyle}"/>
                        </Grid>
                        <ListView ItemsSource="{Binding Emotions}" BackgroundColor="White" Grid.Row="1" Opacity="0.8" SelectedItem="{Binding SelectedEmotion}">
                        </ListView>
                        <Button Grid.Row="2" Style="{StaticResource ButtonStyle}" Command="{Binding RemoveSelectedCommand}" Margin="0" Text="Remove Selected"></Button>
                    </Grid>
                </controls:HeaderBlock>

                <!--Descripition Box-->
                <controls:HeaderBlock Grid.Row="2" HeaderText="Description" >
                    <Frame Opacity="0.8" Padding="4">
                        <Editor BackgroundColor="White" Opacity=".8" IsSpellCheckEnabled="True" Text="{Binding Description}"
                                  HeightRequest="400" Placeholder="Describe how you feel etc..." VerticalOptions="FillAndExpand" HorizontalOptions="CenterAndExpand"/>
                    </Frame>
                </controls:HeaderBlock>

                <!--Cancel / savey save grid-->
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Style="{StaticResource ButtonStyle}" Command="{Binding BackCommand}" Margin="2" Text="Cancel"/>
                    <Button Style="{StaticResource ButtonStyle}" Command="{Binding SaveLogCommand}" Margin="2" Text="Save" Grid.Column="1"/>
                </Grid>
                
            </Grid>
        </controls:PageBackgroundWithHeader>
    </ContentPage.Content>
</ContentPage>