<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SelfMonitoringApp.Controls.HeaderBlock">
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <Frame BackgroundColor="{StaticResource PrimaryLight}" Margin="5" CornerRadius="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="2"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Text="{TemplateBinding HeaderText}" FontSize="20" FontAttributes="Bold" FontFamily="Roboto" TextColor="White" HorizontalTextAlignment="Center"/>
                    <BoxView Grid.Row="1" BackgroundColor="{StaticResource PrimaryDark}" />
                    <ContentPresenter Grid.Row="2" AbsoluteLayout.LayoutFlags="All"/>

                </Grid>
            </Frame>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</ContentView>