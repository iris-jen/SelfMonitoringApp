<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SelfMonitoringApp.Controls.PageBackgroundWithHeader">
    <ContentView.ControlTemplate>
        <ControlTemplate>
            <Grid BackgroundColor="{StaticResource PrimaryLight}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <BoxView  BackgroundColor="{StaticResource PrimaryDark}" />
                <Label Grid.Row="1" Text="{TemplateBinding HeaderText}" Style="{StaticResource HeaderLabelStyle}"/>
      
                <ScrollView Grid.Row="2">
                    <ContentPresenter  HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"/>
                </ScrollView>
            </Grid>
        </ControlTemplate>
    </ContentView.ControlTemplate>
</ContentView>